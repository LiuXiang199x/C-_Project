#include <iostream>
#include <fstream>
#include "string.h"


int test();


/*
char a[] = "aaaaa";
int length = sizeof(a)/sizeof(char); // length=6

不能这样做来获取 char * 的长度
char *a = new char[10];
int length = sizeof(a)/sizeof(char);

*/